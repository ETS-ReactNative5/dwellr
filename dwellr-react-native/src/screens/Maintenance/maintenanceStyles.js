import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
    title: {
        marginTop: 30,
        marginLeft:20,
        marginRight:20,
        textAlign: 'center',
        fontSize: 30, 
        fontWeight: '400',
        marginBottom: 20
      },

    });

    
  export default styles;